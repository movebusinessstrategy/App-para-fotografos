node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Banco de dados local
*.db
*.sqlite

# WhatsApp sessions (dados sensíveis)
baileys_sessions/
sessions/
# Backups de sessões/credenciais — nunca versionar (contêm dados sensíveis)
*_sessions.backup_*
baileys_sessions.backup*

# Uploads do editor de vídeo (arquivos grandes)
server/uploads/
server/processed/
# Vídeos e uploads locais
server/uploads/
server/processed/
*.mp4
*.mov
*.avi
*.mkv
*.bak
# Backups de código (.backup) e arquivos temporários
*.backup
*.orig

# Scripts de diagnóstico ad-hoc (PII de clientes + side-effects em produção)
wa-diag-*.mjs
wa-try-*.mjs

# Backups privados de migração (dados de clientes; nunca versionar)
private-backups/
workers/**/.wrangler/

# Extratos bancários reais nunca devem entrar no repositório
*.ofx
*.qfx
private-finance/
